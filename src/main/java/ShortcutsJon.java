
public class ShortcutsJon {
	static Integer parseInt(String s) { return Integer.parseInt(s); }
	static int lngToInt(long val) { return Math.toIntExact(val); }
	static double pow(double num, double exponent) { return Math.pow(num, exponent);}
}